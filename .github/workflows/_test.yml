name: _test
on:
  workflow_call
    inputs-hoge:
      required: true
      type: string
      default: ''

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: echo hoge
        run echo ${{inputs.inputs-hoge}}