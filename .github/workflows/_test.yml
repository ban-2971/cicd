name: _test
on:
  workflow_call:
    inputs:
      target:
        required: true
        type: string

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: echo target
        run: echo ${{inputs.target}}
      - name: echo branch
        run: echo ${{ github.ref }}
